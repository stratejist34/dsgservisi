---
import { SERVICES } from "@utils/constants";
import { Droplet, Settings, Wrench, Snowflake, Zap, Cog } from "lucide-astro";
---

<section class="relative bg-slate-900 overflow-hidden py-24">
  {/* Technical Grid Background */}
  <div class="absolute inset-0 pointer-events-none">
    <div
      class="absolute inset-0 opacity-[0.02]"
      style="background-image: linear-gradient(#fff 1px, transparent 1px), linear-gradient(90deg, #fff 1px, transparent 1px); background-size: 40px 40px;"
    >
    </div>
  </div>

  <div class="container relative z-10">
    {/* Section Title */}
    <div class="text-center mb-16" data-reveal>
      <span
        class="text-amber-500 font-mono text-sm tracking-widest uppercase mb-2 block"
        >Teknik Çözümler</span
      >
      <h2 class="text-4xl md:text-5xl font-display font-bold text-white mb-6">
        SERVİS <span
          class="text-transparent bg-clip-text bg-gradient-to-r from-amber-400 to-amber-600"
          >HİZMETLERİ</span
        >
      </h2>
      <p class="text-lg text-slate-400 max-w-2xl mx-auto leading-relaxed">
        En son teknoloji diagnostik cihazları ve fabrika sertifikalı
        prosedürlerle aracınızın performansını garanti altına alıyoruz.
      </p>
    </div>

    {/* High-Tech Grid - Bento Style */}
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
      {
        SERVICES.map((service, index) => (
          <a
            href="/iletisim"
            class="group relative h-full"
            data-reveal
            data-stagger-index={index}
          >
            <div class="relative h-full bg-slate-800/40 border border-white/5 rounded-xl overflow-hidden transition-all duration-500 hover:border-amber-500/30 hover:bg-slate-800/60 group-hover:shadow-[0_0_30px_-10px_rgba(245,158,11,0.15)]">
              {/* Image Area - Reduced height for more technical look */}
              <div class="relative h-48 overflow-hidden border-b border-white/5">
                <div class="absolute inset-0 bg-slate-900/20 group-hover:bg-transparent transition-colors duration-500 z-10" />
                <img
                  src={service.image.replace(".avif", "-480.avif")}
                  alt={service.title}
                  class="w-full h-full object-cover transform transition-transform duration-700 group-hover:scale-110 filter grayscale-[30%] group-hover:grayscale-0"
                  loading="lazy"
                  width="480"
                  height="320"
                />

                {/* Tech Overlay Lines */}
                <div class="absolute inset-0 border-white/10 opacity-0 group-hover:opacity-100 transition-opacity duration-500 pointer-events-none z-20">
                  <div class="absolute top-2 left-2 w-2 h-2 border-t border-l border-amber-500" />
                  <div class="absolute top-2 right-2 w-2 h-2 border-t border-r border-amber-500" />
                  <div class="absolute bottom-2 left-2 w-2 h-2 border-b border-l border-amber-500" />
                  <div class="absolute bottom-2 right-2 w-2 h-2 border-b border-r border-amber-500" />
                </div>
              </div>

              {/* Content Area */}
              <div class="p-6 relative">
                {/* Icon Floating */}
                <div class="absolute -top-8 right-6 w-14 h-14 bg-slate-800 border border-white/10 rounded-lg flex items-center justify-center shadow-xl group-hover:border-amber-500/50 group-hover:text-amber-500 text-slate-300 transition-all duration-300 z-30">
                  {service.icon === "🛢️" && (
                    <Droplet class="w-7 h-7" stroke-width={1.5} />
                  )}
                  {service.icon === "⚙️" && (
                    <Settings class="w-7 h-7" stroke-width={1.5} />
                  )}
                  {service.icon === "🔧" && (
                    <Wrench class="w-7 h-7" stroke-width={1.5} />
                  )}
                  {service.icon === "🔩" && (
                    <Cog class="w-7 h-7" stroke-width={1.5} />
                  )}
                  {service.icon === "❄️" && (
                    <Snowflake class="w-7 h-7" stroke-width={1.5} />
                  )}
                  {service.icon === "⚡" && (
                    <Zap class="w-7 h-7" stroke-width={1.5} />
                  )}
                </div>

                <h3 class="text-xl font-display font-bold text-white mb-3 group-hover:text-amber-500 transition-colors">
                  {service.title}
                </h3>
                <p class="text-sm text-slate-400 leading-relaxed line-clamp-3 mb-4 group-hover:text-slate-300 transition-colors">
                  {service.description}
                </p>

                <div class="flex items-center text-amber-500/80 text-xs font-mono uppercase tracking-wider font-semibold group-hover:text-amber-500">
                  <span class="mr-2">Detayları İncele</span>
                  <svg
                    class="w-4 h-4 transform group-hover:translate-x-1 transition-transform"
                    fill="none"
                    stroke="currentColor"
                    viewBox="0 0 24 24"
                  >
                    <path
                      stroke-linecap="round"
                      stroke-linejoin="round"
                      stroke-width="2"
                      d="M17 8l4 4m0 0l-4 4m4-4H3"
                    />
                  </svg>
                </div>
              </div>
            </div>
          </a>
        ))
      }
    </div>

    {/* Footer Link */}
    <div class="text-center mt-16" data-animate>
      <a
        href="/hizmetlerimiz"
        class="group inline-flex items-center gap-3 px-8 py-4 bg-white/5 border border-white/10 rounded-lg font-display font-bold text-white hover:bg-amber-500 hover:border-amber-500 hover:text-slate-950 transition-all duration-300"
      >
        <span>TÜM HİZMET LİSTESİ</span>
        <svg
          class="w-5 h-5"
          fill="none"
          stroke="currentColor"
          viewBox="0 0 24 24"
          ><path
            stroke-linecap="round"
            stroke-linejoin="round"
            stroke-width="2"
            d="M13 7l5 5m0 0l-5 5m5-5H6"></path></svg
        >
      </a>
    </div>
  </div>
</section>
